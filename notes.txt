<?php

// Databae configuration
$dbHost = "127.0.0.1";
$dbUser = "root";
$dbPass = "Darrellerevis24";
$dbName= "mapdb";

// Create database connection
$db = new mysqli($dbHost, $dbUser, $dbPass, $dbName);

// Check database connection
if ($db->connect_error) {
    die("Connection failed: " . $db->connect_error);
}

?>